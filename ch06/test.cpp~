#include "../std_lib_facilities.h"

int main()
{
    string ins;
    cout << ">> ";
    cin >> ins;
    int n;
    for (char c : ins) {
        if (isdigit(c)) {
            n = n*10 + (c - '0');
        }
        else {
            break;
        }
    }
    
    return 0;
}
